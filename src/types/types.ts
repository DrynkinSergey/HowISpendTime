export interface TimerProps {
    id: number,
    title: string,
    time: string,
    color: string,
}
export interface TimerI {
    id: number,
    title: string,
    time: string,
    color: string,
}